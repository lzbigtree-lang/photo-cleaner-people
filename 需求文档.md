# MVP 需求文档：照片人物清除工具

**版本：** v0.1  
**日期：** 2026-03-17  
**作者：** Gavin  

---

## 一、产品概述

一个纯静态网页工具，用户上传一张照片，点击想要**保留的人物**，系统自动去除照片中其他所有人，背景保持不变，最终输出处理后的图片。

---

## 二、用户故事

> 作为用户，我上传一张合影，点击我自己，系统自动把其他人从照片中移除，背景完整保留，我可以下载处理后的图片。

---

## 三、功能范围（MVP）

### 3.1 核心功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 图片上传 | 支持 JPG/PNG，最大 10MB | P0 |
| 点击选人 | 在预览图上点击要保留的人物 | P0 |
| 人物分割 | 调用 SAM 2 识别点击位置的人物轮廓 | P0 |
| 人物消除 | 调用 LaMa 对其他人物区域做背景修复 | P0 |
| 结果预览 | 展示处理前后对比 | P0 |
| 图片下载 | 下载处理后的图片 | P0 |

### 3.2 MVP 不包含

- 用户账号 / 登录
- 图片存储 / 历史记录
- 批量处理
- 移动端适配（基础响应式即可）
- 多人保留（MVP 只支持保留一个人）

---

## 四、技术方案

### 4.1 架构

```
纯静态前端 (HTML + Vanilla JS)
        │
        ├── Replicate API: SAM 2
        │   输入：图片 + 点击坐标
        │   输出：人物 mask（遮罩图）
        │
        └── Replicate API: LaMa
            输入：原图 + 需消除区域的 mask
            输出：修复后的图片
```

### 4.2 API 选型

| 用途 | 模型 | Replicate 地址 |
|------|------|---------------|
| 点击分割 | SAM 2 | `meta/sam-2` |
| 背景修复 | LaMa | `advimman/lama` |

### 4.3 数据流

1. 用户上传图片 → 转 base64，存内存
2. 用户点击图片 → 记录点击坐标（相对于原图尺寸）
3. 调用 SAM 2 → 传入图片 + 坐标 → 返回**保留人物的 mask**
4. 对 mask 取反 → 得到**需要消除的区域 mask**
5. 调用 LaMa → 传入原图 + 消除 mask → 返回修复图
6. 展示结果，提供下载

### 4.4 API Key 处理

MVP 阶段：用户在页面输入自己的 Replicate API Token，存 `localStorage`，不上传服务器。

---

## 五、页面设计

### 5.1 页面结构（单页）

```
┌─────────────────────────────────────┐
│  🖼️ 照片人物清除工具                  │
├─────────────────────────────────────┤
│  [API Token 输入框]  [保存]           │
├─────────────────────────────────────┤
│  [上传图片区域 / 拖拽]                 │
├──────────────┬──────────────────────┤
│  原图预览     │  处理结果预览          │
│  (可点击选人) │  (处理中显示 loading) │
├──────────────┴──────────────────────┤
│  提示文字：点击左侧图片中要保留的人物   │
│  [重新选择]  [下载结果]               │
└─────────────────────────────────────┘
```

### 5.2 交互流程

```
上传图片
  → 显示原图预览，提示"点击要保留的人"
  → 用户点击人物
  → 显示 loading，调用 SAM 2
  → SAM 2 返回 mask，高亮显示选中人物轮廓
  → 自动调用 LaMa 处理
  → 显示处理结果
  → 用户可下载 / 重新选择
```

---

## 六、错误处理

| 场景 | 处理方式 |
|------|---------|
| API Token 未填写 | 提示填写 Token |
| 图片超过 10MB | 提示压缩后上传 |
| SAM 2 未识别到人物 | 提示"未检测到人物，请重新点击" |
| API 调用失败 | 显示错误信息，提供重试按钮 |
| 处理超时（>60s） | 提示超时，建议缩小图片尺寸 |

---

## 七、验收标准

- [ ] 可上传 JPG/PNG 图片
- [ ] 点击人物后有视觉反馈（高亮轮廓）
- [ ] 处理完成后展示结果图
- [ ] 结果图中目标人物完整保留，其他人物消失
- [ ] 背景区域无明显修复痕迹（LaMa 效果）
- [ ] 可下载结果图
- [ ] API Token 本地保存，刷新不丢失

---

## 八、后续迭代方向（Post-MVP）

- 支持保留多个人（多次点击）
- 移动端优化
- 处理进度条
- 支持撤销 / 重做
- 接入更好的 inpainting 模型（如 SD Inpainting）
- 可选：后端代理 API Key，避免用户自备

---

## 九、开发估时（参考）

| 模块 | 估时 |
|------|------|
| 页面布局 + 上传 | 2h |
| 点击交互 + 坐标计算 | 2h |
| SAM 2 API 集成 | 3h |
| LaMa API 集成 | 2h |
| 结果展示 + 下载 | 1h |
| 错误处理 + 调试 | 2h |
| **合计** | **~12h** |
